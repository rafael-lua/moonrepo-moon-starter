language: "typescript"

project:
  name: "Hello World Module"
  description: "A example module."
  channel: ""
  owner: ""
  maintainers: []

tasks:
  build:
    command: "tsdown"
    inputs:
      - "src/**/*"
    outputs:
      - "dist"
    deps:
      - "^:build"
